"ray[default]"
"ray[air]" # installs Ray + dependencies for Ray AI Runtime
"ray[tune]"  # installs Ray + dependencies for Ray Tune
"ray[rllib]"  # installs Ray + dependencies for Ray RLlib
"ray[serve]"  # installs Ray + dependencies for Ray Serve
gymnasium
numba==0.56.4
numpy==1.23.5
scipy==1.9.1
torch==2.0.0
torch-tb-profiler==0.4.1
torchaudio==2.0.1
torchvision==0.15.1
tensorboard